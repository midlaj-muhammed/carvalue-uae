[project]
name = "ml"
version = "1.0.0"
description = "ML training pipeline for CarValue UAE"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "pandas",
    "scikit-learn",
    "xgboost",
    "joblib",
]
